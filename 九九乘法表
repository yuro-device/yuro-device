#include <stdio.h>
int main() {  
    for (int i = 1; i <= 9; i += 1) {
        for (int j = 1; j <= i; j += 1){
            printf("%d * %d = %2d ", j, i, i * j);
        }
        printf("\n");
    }
    return 0;
}
